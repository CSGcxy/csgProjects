<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cxy.netflowservice.mapper.FlowsMapper">

    <select id="getActiveFlows" resultType="com.cxy.netflowservice.entity.vo.ActiveFlowsVO">
        SELECT APP_PROTO,PROTO,SRC_IP,DST_IP,DURATION,FRONT_RATE,BACK_RATE,BYTE_COUNT
        FROM ${segment} WHERE TIMESTAMP = (SELECT MAX(TIMESTAMP ) FROM ${segment}) LIMIT 6
    </select>

    <select id="getFlowSankey" resultType="com.cxy.netflowservice.entity.FlowSankey">
        SELECT SRC_IP, DST_IP, BYTE_COUNT
        FROM Flows ORDER BY BYTE_COUNT DESC LIMIT 10
    </select>


</mapper>
